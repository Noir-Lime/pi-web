---
"@jmfederico/pi-web": patch
---

Show all matching slash commands and skills in scrollable autocomplete instead of limiting results to twelve. Display prompt-template argument hints beside commands and as editor ghost text after selection, including hints for `/name` and `/compact`.

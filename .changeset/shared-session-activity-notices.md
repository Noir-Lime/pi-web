---
"@jmfederico/pi-web": patch
---

Show an advisory notice when a shared session was recently active in another PI WEB instance, helping users avoid working on the same conversation in both instances at once. Detection is best-effort and does not lock the session or prevent concurrent work.
